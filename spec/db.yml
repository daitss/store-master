# The databases and access that must exist prior to doing the db-based spec tests:
#
store_master_mysql:     { vendor: mysql,    hostname: localhost, database: store_master_spec_test, username: fischer, password: topsecret }
store_master_postgres:  { vendor: postgres, hostname: localhost, database: store_master_spec_test, username: fischer, password: topsecret }

store_daitss_mysql:     { vendor: mysql,    hostname: localhost, database: store_master_daitss_spec_test, username: fischer, password: topsecret }
store_daitss_postgres:  { vendor: postgres, hostname: localhost, database: store_master_daitss_spec_test, username: fischer, password: topsecret }
